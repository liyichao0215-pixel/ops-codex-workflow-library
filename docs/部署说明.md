# 部署说明

## 目标

第一阶段部署成内部内测版：同事打开一个 URL 可以提交投稿，管理员审核通过后发布为正式资产。默认仍可用 SQLite 做无绑卡演示；正式联动版使用飞书多维表格保存资产、投稿和审核记录。

## M1 推荐架构

```text
最快内测：一个 Render Web Service
  同一个 URL 同时提供前端页面和后端 API

后续扩展：GitHub Pages / Vercel 静态前端 + Render / Railway / Fly.io 后端
  适合内测稳定后再拆分
```

M1 先推荐单服务，是因为同事只需要打开一个 URL，不需要理解“前端地址”和“后端地址”的区别。

## Render 一键部署准备

项目根目录已经提供：

```text
render.yaml
```

当前 `render.yaml` 默认走无绑卡临时内测模式：创建一个 Free Node Web Service，并把 SQLite 写到临时目录：

```text
DATA_SOURCE=sqlite
OPS_DB_PATH=/tmp/ops-assets.sqlite
```

这个模式用于先让同事打开网站、提交投稿、走通审核和发布流程。注意：Render Free Web Service 不支持 persistent disk，服务重启或重新部署后，临时 SQLite 数据可能丢失。

如果要切到飞书联动版，把 Render 环境变量改成：

```text
DATA_SOURCE=feishu
FEISHU_APP_ID=飞书应用 App ID
FEISHU_APP_SECRET=飞书应用 App Secret
FEISHU_BASE_APP_TOKEN=飞书多维表格 app_token
FEISHU_ASSETS_TABLE_ID=工作流资产表 table_id
FEISHU_SUBMISSIONS_TABLE_ID=投稿审核池 table_id
FEISHU_DUPLICATES_TABLE_ID=重复与更新记录 table_id
FEISHU_AUDIT_TABLE_ID=审核日志与管理员表 table_id
FEISHU_WIKI_HOME_URL=https://your-team.feishu.cn/wiki/replace-with-your-home
```

如果 `DATA_SOURCE=feishu` 但关键表 ID 没有填齐，服务会回退到 SQLite，避免线上页面直接不可用。

Render 官方文档说明：Blueprint 默认使用仓库根目录的 `render.yaml`；Free Web Service 支持免费部署，但不支持 persistent disk。持久盘只能挂到付费 Web Service，并且只有挂载路径下的数据会在部署和重启后保留。

## Render 手动步骤

1. 在 GitHub 新建并推送这个仓库。
2. 打开 Render，选择 New Blueprint。
3. 选择这个 GitHub 仓库。
4. 确认 `render.yaml` 被识别。
5. 设置 `ADMIN_TOKEN`，这是管理员发布资产时用的审核密码。
6. 部署完成后打开：

```text
https://ops-codex-workflow-library.onrender.com/
```

7. 检查健康接口：

```text
https://ops-codex-workflow-library.onrender.com/api/health
```

也可以直接运行 M1 smoke test：

```bash
node scripts/m1-smoke-test.mjs https://ops-codex-workflow-library.onrender.com
```

它会验证首页、健康接口、启动数据和至少 10 条正式资产。

如果 Render 分配的域名不是上面这个，需要同步修改：

```text
PUBLIC_ORIGINS=你的实际 Render 域名
```

## 后端环境变量

```text
PORT=8787
ADMIN_TOKEN=只给管理员使用的审核密码
PUBLIC_ORIGINS=https://你的前端域名
DATA_SOURCE=sqlite
OPS_DB_PATH=/data/ops-assets.sqlite
FEISHU_APP_ID=飞书应用 App ID
FEISHU_APP_SECRET=飞书应用 App Secret
FEISHU_BASE_APP_TOKEN=飞书多维表格 app_token
FEISHU_ASSETS_TABLE_ID=工作流资产表 table_id
FEISHU_SUBMISSIONS_TABLE_ID=投稿审核池 table_id
FEISHU_DUPLICATES_TABLE_ID=重复与更新记录 table_id
FEISHU_AUDIT_TABLE_ID=审核日志与管理员表 table_id
FEISHU_WIKI_HOME_URL=https://your-team.feishu.cn/wiki/replace-with-your-home
```

本项目无绑卡临时内测默认使用：

```text
DATA_SOURCE=sqlite
OPS_DB_PATH=/tmp/ops-assets.sqlite
PUBLIC_API_BASE=same-origin
```

## 后端健康检查

```text
GET /api/health
```

成功时返回：

```json
{
  "ok": true,
  "mode": "realtime-backend",
  "dataSource": "sqlite"
}
```

切到飞书后，`dataSource` 应返回 `feishu`。

## 前端接入云后端

如果是单服务部署，不需要改页面，`apps/web/config.js` 默认使用：

```text
same-origin
```

如果是 GitHub Pages / Vercel 静态前端，修改或生成 `apps/web/config.js`：

```js
window.OPS_CONFIG = {
  "apiBase": "https://你的后端服务地址"
}
```

也可以用命令生成：

```bash
PUBLIC_API_BASE=https://你的后端服务地址 node scripts/write-web-config.mjs
```

本地访问 `127.0.0.1` 或 `localhost` 时，前端仍默认连接 `http://127.0.0.1:8787`。

## 数据持久化

无绑卡临时内测不满足数据持久化验收，只适合先验证页面、投稿、审核和发布主流程。

正式 M1 内测如需保留投稿和审核数据，必须配置持久磁盘，并把 `OPS_DB_PATH` 指向持久磁盘路径。不要只使用临时容器目录。

Render 持久盘需要付费 Web Service。建议配置：

```text
plan=starter
mountPath=/opt/render/project/src/storage
sizeGB=1
OPS_DB_PATH=/opt/render/project/src/storage/ops-assets.sqlite
```

## 备份

最低要求：

- 发布内测前手动备份一次。
- 每天自动备份一次 SQLite 文件或确认云平台持久盘快照策略。
- 每次管理员批量发布前导出正式资产。

本地备份命令：

```bash
node scripts/backup-sqlite.mjs
```

本地导出命令：

```bash
node scripts/export-assets.mjs
```

Render 的持久盘会有平台快照，但正式资产仍建议定期导出，避免只依赖整盘恢复。

## 飞书数据源建表

飞书联动版需要在同一个飞书多维表格里准备 4 张表：

- 工作流资产表
- 投稿审核池
- 重复与更新记录
- 审核日志与管理员表

字段名以 [飞书数据源联动版设计](superpowers/specs/2026-06-11-feishu-data-source-design.md) 为准。上线前先保持 `DATA_SOURCE=sqlite`，等飞书应用权限、app_token 和 table_id 都确认后，再切到 `DATA_SOURCE=feishu`。
